"""Single source of truth for the tool version.

The version string is embedded in every export's forensic manifest so that an
output artifact can always be tied back to the exact tool revision that
produced it.
"""

__version__ = "0.1.0"
TOOL_NAME = "micap-imessage-export"
