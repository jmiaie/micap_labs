# Copy to .env and fill in. NEVER commit the real file (.gitignore covers it).
# Required only for live mode (M4); paper/backtest need nothing.
POLYMARKET_PRIVATE_KEY=
POLYMARKET_PROXY_ADDRESS=
# Optional sharp odds anchor (strategy E2)
ODDS_API_KEY=
# Overrides config bankroll_usd when set
BANKROLL_USD=
